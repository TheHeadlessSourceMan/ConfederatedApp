"""
Authenticate that a machine is what it claims to be
"""
from machineIdentity import MachineIdentity


def authenticateMachine(
    machine:MachineIdentity,
    authenticationKey:str)->bool:
    """
    Authenticate that a machine is what it claims to be
    """
    return True # TODO: later
